{
  "name": "css-battle-toolbox",
  "version": "1.1.5",
  "author": "Evan Liomain",
  "license": "MIT",
  "description": "Some usefull tools for Css Battle",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp src/options.js dist/src && cp -R images dist && zip -r css-battle-toolbox.zip dist",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "prettier": "^3.6.2",
    "typescript": "^4.9.5",
    "vite": "^5.4.20",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "taninsam": "^1.17.0"
  }
}
